# Contributing

These notes are mostly intended for myself :).

## Release process

```
git checkout candidate
# <hack code>
git commit -m "New stuff"
git tag candidate/<N>
git push
git push --tags
```
